<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.SpeakEazy" parent="Theme.AppCompat.Light.NoActionBar" />
    <style name="Theme.SpeakEazy.Splash" parent="Theme.SplashScreen">
        <!-- Optional: remove status bar until transition -->
        <item name="windowSplashScreenBackground">#291433</item>
        <item name="postSplashScreenTheme">@style/Theme.SpeakEazy</item>
    </style>
</resources>